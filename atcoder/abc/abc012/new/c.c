#include <stdio.h>

#define MAX(a,b) ((a)>(b)?(a):(b))
#define MIN(a,b) ((a)<(b)?(a):(b))

int main(void){
    int n, h, a, b, c, d, e;
    scanf("%d%d%d%d%d%d%d,&n,&h,&a,&b,&c,&d,&e);

    long ans = 1L<<30;
    return 0;
}
